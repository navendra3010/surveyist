import 'package:flutter/material.dart';

class ManagerdashboardPage extends StatelessWidget {
  const ManagerdashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar:AppBar(),
      body:Center(
        child:Text("Manger  panel"),
      ),
    );
  }
}